.DS_Store
node_modules/

# Container identity + secrets (docker --env-file) — never committed
.devcontainer/devcontainer.env

# Agent scratch space, if you clone repos under the workspace
/work/
/projects/
.claude/settings.local.json

# Orchestrator runtime artifacts (per CLAUDE.md — never committed)
/logs/
.worker-done
.worker-done.prev
.worker-*
STOP
.release-done
.orchestrator.pid

# Estudio app
.env
/data/
dist/
*.tsbuildinfo
docs/fixtures/lesson-audio
docs/mochi-ui-screens