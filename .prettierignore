# Markdown files here are agent-facing templates; formatters mangle the
# fill-in bullets and break orchestrate's unfilled-section greps.
*.md

# Off-limits to workers; never reformat
.devcontainer/
bin/
docs/fixtures/
