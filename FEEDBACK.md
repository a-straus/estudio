# Feedback

<!--
Your inbox to the orchestrator — the steering channel for everything that
does not change what the product IS (that's GOAL.md, which only you edit).

Add items under ## Inbox any time — while it runs or between runs. Bugs,
tweaks, "make this better", feature asks within scope. Locally: edit and
save. Remote (ORCH_SYNC=1): edit on GitHub and commit.

Every iteration the orchestrator empties the inbox: each item becomes
prioritized backlog tasks (or a schema-gate cycle if it touches the data
model), then moves to ## Processed with a disposition note. Items that
would cross a GOAL.md §3 non-goal are escalated to QUESTIONS.md instead of
silently acted on or ignored. The loop will not declare Release done while
the inbox is non-empty.

Altitude guide:
  "the conjugation drill should shuffle answers"     → here
  "add a listening-comprehension mode"               → here (becomes an epic)
  "the product should also teach French"             → GOAL.md (changes the goal)
-->

## Inbox

<!-- - one item per dash; date them if you like -->
<!-- (empty — the KJV-cost item was processed in iteration 165; see ## Processed) -->

- I dont see any of the mochi words. Also, i dont' see anythign listed about KJV. In fact, theres nothing in ENG at all! What happened here? We spent a lot of time on ENG and KJV and Mochi and I have nothing here to show for it. Please explain
## Processed

- 2026-06-13 (iteration 165) — owner: *"what were you using to process KJV? It looks like opus. That cost me a good amount! You don't think sonnet would have been fine for that? Just try and use cheaper models in the future to do large operations like that, or at least ask before executing."* → **You're right on both counts — acted this iteration.** You were on **opus**: the §14 KJV proof run cost **$7.41** (full book, 46 LLM calls). Model routing is delegated to me (GOAL §13), so I switched the book-scale `gutenberg_extraction` task **opus → claude-sonnet-4-6** (orchestrator-applied, `server/src/llm/service.ts`, one line + a test; mirrors the FABLE-DISABLED precedent). The keep/drop word classifications all pass through your **triage** (triage IS the human check, GOAL §16), so this path doesn't need the top model the way definitions/cloze do. **Honest number:** sonnet is ~0.6× opus's price (not ~⅕), so the same full KJV now runs ~**$4.45** — ~40% less, and back under the §13 $5 confirm gate. (Cheaper still = haiku, but you named sonnet and classification quality still feeds triage, so I didn't undercut your call — say the word if you want haiku.) Pinned to a literal so it stays sonnet even when Fable returns. **Second half of your ask ("ask before executing"):** the reason the $7.41 run didn't trip the >$5 in-app confirm is the upfront estimate **undershot** (it guessed $4.84; real was $7.41 — it modeled 20 output-tokens/candidate, reality was 31). → queued **gutenberg-cost-estimate-headroom** (In Progress, sonnet) to recalibrate the estimate to the real per-candidate token counts + headroom, so the >$5 confirm fires reliably before any genuinely expensive ingest. I did **not** re-run the KJV on sonnet just to re-prove §14 — the pipeline proof is model-independent and already clean; re-running would be exactly the needless large spend you flagged. [done: model switch orchestrator-applied + estimate-headroom task spawned; see DECISIONS iter 165]


- 2026-06-13 (iteration 149) — owner: *"Anthropic has disabled access to Fable (U.S. government directive). Your first task this run: remove mentions of Fable from documentation, spawn executables, et cetera, everywhere in the repo — but note where you make the changes so we can switch back without much effort when Fable returns. Otherwise we're on the right track, keep going."* → **DONE this iteration (orchestrator-applied directly, no worker; commit ec4bc5c).** This was urgent, not just doc hygiene: **8 quality-critical LLM tasks defaulted to `claude-fable-5` in `server/src/llm/service.ts` and would now fail at runtime.** Switched all 8 (`pdf_extraction`, `page_classification`, `text_extraction`, `word_definition`, `grammar_curriculum`, `grammar_lesson`, `quiz_cloze`, `lesson_analysis`) to **`claude-opus-4-8`** — the strongest model still available — via a single `FABLE_REPLACEMENT` constant, so **reversion is one line** (flip it back to `"claude-fable-5"`). Every change site carries a grep-able **`FABLE-DISABLED`** marker (run `grep -rni FABLE-DISABLED` to find all of them): service.ts (defaults), service.test.ts (assertion), anthropic.ts (kept the fable-5 *pricing* entry, marked, so a pinned override still costs right + reversion needs no edit there), README.md (orchestrator routing table), docs/README.md (hot-swap proof + default list), docs/validation-pdf-ingestion.md (historical run marked — *not* rewritten, since it faithfully records a run that used fable-5), ARCHITECTURE.md (+ Change-log). `check.sh` green (613 tests). Model defaults are delegated to the orchestrator (GOAL §13), so no GOAL conflict — and GOAL.md names no model. **Deferred (cosmetic, comment-only):** two inert `ORCH_MODEL=fable` *example* comments in `bin/orchestrate`'s header — that file is the live running loop, and editing bytes above its execution point risks corrupting the in-flight process; the actual default is already `ORCH_MODEL=opus` (line 79), so nothing functional uses Fable there. (Historical mentions in TASKS.md/DECISIONS.md append-only memory + gitignored logs/ are left as-run records.) [done — orchestrator-applied; see DECISIONS iter 149]

- 2026-06-12 (iteration 145) — owner: *"iter-XXX.json doesn't help me — before terminating the run, pass it to a lightweight model (Sonnet, low thinking), 'summarize this run for the human', so there's a couple of readable paragraphs in a .md/.txt beside the JSON and I don't have to copy-paste it into a chat window."* → **BUILT this iteration (orchestrator-applied, no worker).** This is harness infra, not product/worker work, so I did it directly — and a container rebuild won't wipe it (it's committed to the repo). New **`bin/iter-summary <iter-json> <out-md> [model]`**: pulls the iteration's status report (`.result`) out of `logs/orchestrator/iter-N.json`, sends it to `claude -p --model sonnet --effort low` with a "rewrite for a busy human in 1–3 plain-English paragraphs, lead with the outcome, no headings/bullets" prompt, and writes **`logs/orchestrator/iter-N.md`** beside the JSON. It self-authenticates by sourcing `.devcontainer/devcontainer.env` when no token is in the env (mirrors `bin/spawn`'s worker-auth), is time-boxed (`timeout 180`), and is fully best-effort — any missing tool / model error / empty input leaves no file and exits 0. Wired into `bin/orchestrate`'s success path as a single `|| true` call (below the loop's live execution point, syntax-checked with `bash -n`), so it can never stall or break the loop. **Tested end-to-end** against iter-144.json → produced an accurate 2-paragraph summary in ~8s. Active for every iteration from here on (and ready for the post-rebuild ffmpeg runs); override the model via `ORCH_SUMMARY_MODEL`. [built — orchestrator-applied harness change; see DECISIONS iter 145]

- 2026-06-12 (iteration 145) — owner: *"let anything in flight finish, don't start anything new — I have to rebuild the container to give you FFMPEG; next run or two just let everything finish; if you have extra runs just stop and let me rebuild so you can keep going on FFMPEG."* → **ACTED: integrated the one in-flight worker, spawned ZERO new work, now holding for the rebuild.** ask-mobile-composer (the last task in flight) FINISHED → verified its own commit abb7f68 = exactly Ask.tsx + Ask.css (the rest of the range-diff was fork-drift) → integrated → main green **596**. Per the directive I spawned nothing new: the held feature-wave tasks (mochi-yesno-review, suggestion-streaming, quick-add-dictation) stay queued. To honor "if you have extra runs just stop," I placed a **`STOP` file** so the loop halts cleanly after this iteration (instead of burning idle holding passes) — the container can now be rebuilt with ffmpeg with **0 workers running = no worktree wiped**. **To resume after the rebuild: `rm STOP` then re-run `bin/orchestrate`** (the loop prints this same instruction when it exits on STOP). The instant ffmpeg is live in-container I resume with the two Phase-2 done-gate tasks (lesson-audio-oversized-splitting + real-recording validation), then the rest of the wave. The run-summary feature above was the one thing I did do — harness infra (not worker work), useful to have in place before the ffmpeg runs. [acted: integrate-and-hold for the rebuild]

- 2026-06-12 (iteration 143) — owner: *"I put the UI screens in the /docs/mochi-ui-screens"* → **acknowledged; reference material for the already-queued `mochi-yesno-review` task, not a new ask.** Verified: 5 Mochi screenshots (`IMG_5762`–`IMG_5766.PNG`, ~1 MB) are now present in `docs/mochi-ui-screens/` — filling the gap iter-142 flagged (the dir was empty when the task was queued, so v1 was to be built from text + identity). **mochi-yesno-review stays queued and still blocked on `review-flow-polish`** (both write `Review.tsx`; can't run two writers on one file) — capacity is full and the collision stands, so nothing to spawn this pass. When review-flow-polish lands and a slot frees, I'll study these 5 screens, amend `review.md` + `components.md` from them (the binary "Do you know it?" card + Yes/No controls), commit, then spawn mochi-yesno-review pointed at the amended design files (workers build from design/*.md, not raw PNGs — I translate the screens into the contract). Also **fixed the `.gitignore` path** the owner added — `docs/fixtures/mochi-ui-screens` → `docs/mochi-ui-screens` — so the screenshots are ignored at their actual location (matching the `docs/fixtures/lesson-audio` precedent for binary reference assets; I can still read them directly to inform the design amendment). [reference material → mochi-yesno-review, still blocked on review-flow-polish]

- 2026-06-12 (iteration 142) — **owner dropped an 11-item batch.** Checked every item against GOAL §3 non-goals and §13 triggers: **10 are in scope → queued as Backlog tasks** (3 spawned this iteration); **1 (mobile /ingest guard) contradicts a GOAL §15 done-criterion → escalated**, not silently built. Dispositions:
  - **[UX - Review Flow] remove "Check Answer" on MC** → folded with the next item into **review-flow-polish** (SPAWNED this iter). Confirmed by reading the code that /review still has the "Check answer" button on both MC and cloze cards (the earlier fix only touched /quiz, which already grades on click). [Should — daily-touch]
  - **[UX - Navigation Flow] Home "Start Review" launches the session directly** → folded into **review-flow-polish** (SPAWNED). Home's "Start review" → /review → the landing "{N} due · Start review" page; the task adds an autostart so the Home CTA jumps straight into the active run. Merged with the item above because both touch Review.tsx (can't run two writers on one file). [Should]
  - **[Feature - Data Management] single-click delete of /ask history entries (no confirm)** → **ask-history-delete** (SPAWNED). Adds `DELETE /api/chat/threads/:id` (no such endpoint today) + a trailing `×` on thread-list rows; chat_message has no ON DELETE CASCADE, so the query deletes messages then the thread in a transaction (no schema change). [Should]
  - **[Architecture - Refactor Scope] MPA→SPA scoping assessment** → **spa-routing-assessment** (SPAWNED, read-only, strong model). Confirmed real: App.tsx routes on `window.location.pathname` with plain `<a href>`/`window.location.assign` — every nav IS a full-page reload. The task writes `docs/spa-routing-assessment.md` (current architecture, why reloads happen, conversion options + effort/risk within §8's React+Vite / §3 no-native-app bounds); the refactor itself is a separate future decision. [Should]
  - **[Feature - Review UI] Mochi-style binary "Do you know it?" review format (per-deck/session toggle)** → **mochi-yesno-review** [Should, Backlog]. In scope (MC stays default; Yes/No is an opt-in render mode mapping Yes→knew-it / No→didn't-know). **Note:** you pointed at `/docs/mochi-ui-screens`, but that directory is **empty** (and the `.gitignore` line you added says `docs/fixtures/mochi-ui-screens`, a different path than the `docs/mochi-ui-screens` dir that exists) — the screens aren't in the repo yet. I'll build a v1 from the text + our dictionary identity and refine if the screens land; drop them in `docs/mochi-ui-screens/` (and fix the .gitignore path if you want them ignored) whenever convenient.
  - **[Feature - Global Navigation] persistent "+" quick-add (word/phrase) in the bottom nav, mobile + desktop, opens a modal** → **quick-add-modal** [Should, Backlog]. In scope (§6.2 CRUD + §3's "word-creation behind a clean internal API"). Touches the nav (AppNav/AppShell) + a new modal component + the words API; I'll amend the design contract (a QuickAdd modal + the nav "+" placement) before spawning.
  - **[Feature - Audio Ingestion] mic dictation inside the quick-add modal** → **quick-add-dictation** [Should, Backlog, depends on quick-add-modal]. Audio *input* is in scope (only TTS/audio output is deferred, §3); reuses the existing transcription layer.
  - **[Performance - LLM Optimization] downsize primary LLM + stream suggestion generations** → **llm-latency-tuning** + **suggestion-streaming** [Should, Backlog]. Model swaps are delegated to me (§13). I'll downshift the latency-sensitive interactive paths (suggestions, chat) to a faster model and add streaming to suggestion generation, while keeping the quality-critical batch paths (vision extraction, classification, free-text grading) strong so the §3 C1-accuracy / §14 ≥90%-accepted goals aren't sacrificed for speed. Streaming touches the LlmProvider seam (additive, no provider leakage).
  - **[UI - Bottom Nav] taller mobile bottom nav + dividers/contrast between items** → **nav-height-dividers** [Should, Backlog]. Supports §7's ≥44px tap targets. I'll amend shell.md (AppNav height/divider spec, within the identity — quiet, not gamified) before spawning.
  - **[UI - Ask Page] redesign mobile /ask composer (full-width input, mic top-right, send bottom-right)** → **ask-mobile-composer** [Should, Backlog]. In scope (mobile-primary Ask, §6.5). I'll amend ask.md's composer region before spawning. Sequenced after ask-history-delete (both touch Ask.tsx).
  - **[Guardrail - Mobile Access] disable /ingest on mobile** → **ESCALATED** (QUESTIONS.md "[PENDING] Disable /ingest on mobile"). It contradicts GOAL §15's "every Phase-1 Must story passes on a real phone browser" (PDF ingestion is a Phase-1 Must). GOAL.md is yours to change, so I'm confirming you want to waive that sub-criterion rather than silently overriding it. Task **ingest-mobile-guard** is queued Blocked pending your answer; I build it the moment you pick an option.



- 2026-06-12 (iteration 139) — "cannot view the website to review it … `npm run build && NODE_ENV=production npm start` used to serve localhost:3000, now I get nothing" → **investigated; the committed code's production build + serve work end-to-end at HEAD (466614a) — this is NOT a code defect.** Evidence gathered this iteration by tool: a clean `rm -rf */dist && npm run build` exits **0** and emits `web/dist/index.html` + assets + `server/dist`; booting `NODE_ENV=production node server/dist/index.js` and probing it returned **200 for `/` (real index.html), 200 for `/api/health` (`{"status":"ok"}`), and 200 for the JS bundle** — the page loads correctly. So the serve path is fine; "nothing" is environmental. Prime suspect: the **`&&` chain** — if `npm run build` prints *any* error, `npm start` never runs (so :3000 has nothing), and a recent devcontainer rebuild (the ffmpeg one) commonly leaves `node_modules` / the native `better-sqlite3` binding needing reinstall, which makes the build (or boot) fail silently behind the `&&`. **What to do:** run the two halves separately — (1) `npm install` at the repo root (re-link deps after a rebuild), (2) `npm run build` **alone** and confirm it ends without error + prints the vite `dist/…` summary, then (3) `NODE_ENV=production npm start`, which should log `server listening … port 3000`. If the build is clean but :3000 is still blank: check nothing else holds the port (`lsof -i :3000`) and that you're opening it from the same machine the server runs on (if it's the always-on box, use the LAN/Tailscale address from `docs/README.md`, not `localhost`). If it persists, paste the `npm run build` output + the startup log and I'll pinpoint it. **Also queued a small robustness fix → `prod-serve-web-guard`** (spawned this iteration): make the production server self-diagnosing — log the openable `http://localhost:PORT` on boot, and if the web build is missing return a clear "run `npm run build`" message + a boot warning instead of a blank/opaque page (GOAL §6.9/§12: errors surfaced, never swallowed). [investigated + answered + 1 task]

<!-- Moved here by the orchestrator with what it did about each. -->

- 2026-06-12 (iteration 89) — "we need a hamburger menu or something to nav on mobile" + "navbar disappears on /review" → **mobile-nav-and-review-landing** task [Must]. Root cause found: the design contract (shell.md) already specifies a phone **bottom-bar nav (AppNav)**, and `SiteHeader.css` deliberately hides the header's nav links below 640px expecting AppNav to own phone nav — but **AppNav was never built** (home-nav-footer shipped SiteHeader/SiteFooter only), so phone currently has *zero* navigation. Task builds AppNav per shell.md (the contract's answer to "nav on mobile" is a thumb-zone bottom bar, not a hamburger). The /review complaint is the same nav-loss made acute by Review being a deliberate session takeover: refinement = the active card-answering run stays a focused takeover, but /review's pre-session landing + empty/finished states show standard chrome (header + AppNav + footer), aligning Review with the existing Quiz config→play pattern. shell.md amended + INDEX change-log this iteration. WAIT for schema-gate-003 to integrate (gate runs alone); no schema grant. [Must]
- 2026-06-12 (iteration 89) — "the 'i forgot' button that pops up is on top of the LEARNING text on /library; it should be horizontally on the side" → **library-forgot-button-layout** task [Should — trivial CSS]. The "I forgot this" control overlaps the "LEARNING" status text in the Library word row/detail; lay them out horizontally (button beside the status, no overlap) using existing tokens. File-disjoint from the nav task (Library screen only), so the two parallelize once the gate lifts. WAIT for schema-gate-003 to integrate; no schema grant. [Should]

- 2026-06-11 (iteration 55) — **MASTER DIRECTIVE** "if this is still here, continue into phase 2 … fix the issues … make a list of things to review … then continue phase 2 as much as you can" → Acted on: this supersedes the Phase-1 freeze gate (DECISIONS.md). The 12 issue items below are converted to a prioritized Phase-1-fix backlog (ahead of Phase 2); a Phase-1 review checklist is posted in QUESTIONS.md ("[INFO] Phase 1 review checklist"); Phase 2 spawning is unblocked and resumes once the flagged fixes are in flight / capacity frees.
- 2026-06-11 — "real progress bar on PDF page reading + curriculum building" → **ingestion-progress-and-topic-link** task (per-page / per-topic progress reported by the jobs and rendered on Ingest + Grammar; also fixes review-03 nit#3 90%-stall). [Should]
- 2026-06-11 — "after 'learn' the word should disappear; possible second-word-skipped bug; horrible post-Keep screen; Done should go to Review; /triage?source=1 isn't a proper redirect" → **triage-fixes** task (spawned this iteration). [Must]
- 2026-06-11 — "we need a header nav bar" + "the / url should be navigable, not open to what it does" → folded into the authorized **design polish**: a home/landing overview + global **home-nav-footer** task. [Must — after fixes land]
- 2026-06-11 — "/library mobile: missing the horizontal divider between all/learning in the 2×2 select control" → **library-mobile-separator** task (SegmentedControl row divider on mobile wrap). [Should — trivial]
- 2026-06-11 — "wrong-reason takes too long to generate; use a smaller/faster model — sonnet low thinking, fable is overkill" → **lesson-grading-fixes** (spawned): quiz_grading LLM task → sonnet at low effort; consider streaming/pre-generation. [Must]
- 2026-06-11 — "notes section on answers (correct or incorrect) that the app later uses as context when generating quizzes" → **notes-on-answers** NEW FEATURE; needs a `note` table → routed through the schema gate. [Should — Phase-1 polish, schema-gated]
- 2026-06-11 — "free-response grading sometimes returns an answer unrelated to what I wrote; should consider what the user said; if I'm 'close', figure out the correct way to say what I meant" → **lesson-grading-fixes** (spawned): grading prompt must ground on the user's actual answer + add a "close → here's how you'd say it" rephrase (ties to review-03 S7 Partly-right tier). [Must]
- 2026-06-11 — "/quiz MC should color green/red instantly on click (no Check-Answer button); remove the 'I don't know' option on MC" → **quiz-ux-fixes** (spawned). [Must]
- 2026-06-11 — "/quiz answer pool is limited to ingested content; build a wider bank at generation time so definitions aren't repetitive" → **quiz-caching-and-bank** task (ties to review-03 S1 cache-reuse + nit#10 distractor quality). [Must]
- 2026-06-11 — "lag between 'check answer' and the color is too long; should be instantaneous" → **quiz-ux-fixes** (spawned): MC correctness decided client-side and colored immediately; persistence happens async. [Must]
- 2026-06-11 — "I need a list of everything implemented + what to work through at a checkpoint" → satisfied now by the **Phase-1 review checklist** in QUESTIONS.md; will be regenerated at each future checkpoint. [done this iteration]

- 2026-06-11 — "When you're done with Phase 1, stop and let me review" → Phase-1 review gate recorded in TASKS.md and DECISIONS.md: once the Phase-1 Musts (grammar-lessons-quizzes, review-03, docs-and-demo) are done and trunk is green, the orchestrator posts a [PENDING] "Phase 1 ready for your review" entry in QUESTIONS.md and spawns no Phase-2 work until you answer. (iteration 51)

- 2026-06-10 — Vision-path PDF extraction, default scan-reading model `claude-fable-5` → folded into ARCHITECTURE.md (LLM-layer conventions: per-task model config; `pdf_extraction` task defaults to `claude-fable-5`) and recorded in DECISIONS.md. Validation against `docs/fixtures/workbook/` will be an acceptance criterion on the Phase 1 PDF-ingestion task when the backlog is decomposed (iteration 2 of the first-iterations protocol).
