# Task Board

<!--
Owned and maintained by the orchestrator. Do not edit manually during a run.

Done levels (from GOAL.md §15):
  Task done   — acceptance criteria met, tests pass
  Feature done — all stories shipped, NFRs met, docs updated
  Release done — feature done + success metrics instrumented + GOAL.md §15 release criteria met
                 (this is when the orchestrator stops its loop)
-->

## In Progress

<!-- Format: - **branch-name**: description (spawned: YYYY-MM-DD HH:MM) -->

<!-- iter 148: BOTH Phase-2 workers landed (list-agents-lag — splitting FINISHED, mochi committed 71bb49b during the pass then died before its .worker-done marker). Integrated both → trunk green 608. Hit + fixed a shared/dist cross-worktree contamination (the symlinked node_modules resolves @estudio/shared→/workspace/shared/dist, so a stale mochi-built dist broke splitting's typecheck, and vice-versa — rebuild /workspace/shared/dist to match the branch under test before each integrate; post-merge it self-heals from main). See DECISIONS iter 148. -->

<!-- quick-add-dictation → DONE (spawned + FINISHED + integrated all within iter 148 — fast sonnet worker, full entry in Done; merge cafd6d5, 613 green). -->

- **review-08**: strong-model audit (`--model $ORCH_MODEL --effort medium --include GOAL.md --include design`) of the 4 net-new product surfaces since review-07 (range a806ca7..HEAD: ask-mobile-composer, lesson-audio-oversized-splitting [the correctness-critical ffmpeg SplitAudio — scrutinize chunk-size guarantees/error-handling/cleanup], mochi-yesno-review [reviewFormat KV cross-layer + yesno card], quick-add-dictation [generic transcribe endpoint + dictation wiring]) + the lesson_analysis prompt seed. Writes findings + clean-bill to REVIEW.md. At cadence (review-07 itself audited 4 surfaces; upcoming merge pipeline is thin → audit the Phase-2 done-gate code now, not deferred). Schema: none. (spawned: 2026-06-12 19:55 UTC, iter 148)


## Backlog

### Reviews (cadence)

<!-- review-04 integrated iter 99 (commit b5c9126; REVIEW.md dispositioned + git-rm'd). -->
<!-- review-05 integrated iter 106 (commit 5e8845c; REVIEW.md dispositioned + git-rm'd — see Done + DECISIONS iter 106). 0 blockers, 8 should-fixes, 11 nits + extensive clean-bill. S1-S3/N9/N11→suggestion-fixes (DONE iter 108); S4-S6/S8/N1/N3/N6/N8→chat-ask-fixes (DONE iter 108); S7→lesson-notes-polish part A (schema-gated, Backlog); N5/N10/N8→lesson-notes-nonschema (In Progress, spawned iter 108); N2/N4/N7→recorded no-task. REVIEW.md slot now FREE. -->
<!-- review-06 INTEGRATED iter 117 (commit 820158a; REVIEW.md dispositioned + git-rm'd). Verdict: 0 blockers, 2 should-fix, 4 nits + extensive clean-bill. S1→lessons-duration-rounding (LANDED iter 118, commit 3f0f3e0/merge 8b8c6d2); S2→suggestion-deck-exclusion-fix (LANDED iter 118, commit fdd27d3/merge fc49a2e); N1-N4→recorded no-task (see DECISIONS iter 117). The review-06 fix wave (S1+S2) is now FULLY LANDED. REVIEW.md slot FREE. -->
<!-- review-07 INTEGRATED iter 144 (commit 2eef478; merge a806ca7; REVIEW.md dispositioned + git-rm'd). Verdict: **SHIP — 0 blocker, 0 should-fix, 1 nit + 2 watch** + extensive line-by-line clean-bill. All 4 reviewed surfaces (prod-serve-web-guard, review-flow-polish, ask-history-delete, llm-latency-tuning) traced against real code, CLEAN. Crucially the reviewer CONFIRMED the §14 ≥90%-accept drivers (word_definition + quiz_cloze) were NOT downshifted by llm-latency-tuning → the model swap is accuracy-safe. NIT-1 (prod boot log uses localhost not LAN/Tailscale — reviewer: "not worth a code change"; README covers the LAN/Tailscale address) → recorded, NO task. WATCH-1 (suggestion accept-rate after suggestion_select→sonnet — spot-check when real suggestion history exists; reversible via the setting/env override) + WATCH-2 (Ask answer quality + tool-call reliability after chat→haiku — reversible one-line config revert) → recorded watch items, NO action now (revert via override if usage shows a regression). Slot FREE. review-08 fires ~5 product merges later (quick-add-modal + the feature wave are the next toward that count, or when Phase-2 done-gate work lands). -->
<!-- FEATURE WAVE — FULLY UNBLOCKED (review-07 landed iter 144, 0 should-fix/0 blocker). **SHIPPED iter 144:** quick-add-modal (f10477a) + nav-height-dividers (a659051; AppNav taller + dividers) — 596 green. **SPAWNED iter 144 (in flight):** ask-mobile-composer (mobile /ask composer redesign). **REMAINING (1 slot free → next pass, each needs FOCUSED design prep, deliberately not crammed into the iter-144 tail):** **mochi-yesno-review** — HIGHEST value; needs studying the 5 owner screens (docs/mochi-ui-screens/ IMG_5762–5766.PNG) → amend review.md + components.md (binary "Do you know it?" card + Yes/No), AND a cross-layer `reviewFormat` setting (add a key to AppSettings in shared/src/settings-api.ts + validation in server routes/settings.ts + defaults in db/settings-queries.ts — KV settings, NO migration) + the Yes/No render mode in Review.tsx; **suggestion-streaming** — correctness-sensitive LlmProvider streaming seam (read the seam first; strong model) + Suggestions.tsx; **quick-add-dictation** — mic in the shipped QuickAddModal; needs a generic transcribe endpoint (today the only transcription HTTP entry is the chat voice route) reusing the TranscriptionService, then wire RecordButton→fill the term field. All mutually file-disjoint (Review/settings · llm/Suggestions · QuickAddModal/transcription) and disjoint from the 2 in flight. Amend the named design file + commit BEFORE each spawn. -->
<!-- After each future review lands: read REVIEW.md, convert real findings → Backlog items, `git rm REVIEW.md`, commit. -->
<!-- review cadence -->


### FEEDBACK batch — owner, 2026-06-12 (iteration 142). 11 items: 3 In Progress, 1 escalated, 7 queued here.

<!-- All within GOAL §3/§5/§6 except ingest-mobile-guard (escalated). Many touch shared surfaces — sequencing notes inline. Amend the named design file + commit BEFORE spawning each UI task. -->

<!-- mochi-yesno-review → In Progress (SPAWNED iter 146). review-flow-polish landed iter 143 (Review.tsx free) + ffmpeg hold lifted → unblocked. Studied the 5 owner screens (docs/mochi-ui-screens/ IMG_5762=front, IMG_5763=revealed both-sides; the other 3 are Mochi's card-creation flow, not the review format) → amended review.md §3.2b + components.md ReviewCard yesno mode + INDEX change-log, committed BEFORE the spawn. Scope decided: a persistent `reviewFormat` KV setting (mc default · yesno) toggled from a SegmentedControl on the Review landing (the landing toggle IS the control — no separate System Preferences entry needed; the landing persists via PUT /api/settings); per-deck deliberately deferred (single user, 2 decks). Schema: none (reuses the `setting` table). -->
<!-- quick-add-modal → LANDED iter 144 (merge f10477a, 596 green; full entry in Done). Review-independent; spawned + finished + integrated all within iter 144. nav-height-dividers + quick-add-dictation (both were blocked on it) are now UNBLOCKED. ingest-mobile-guard still escalated on the /ingest QUESTION (its AppNav collision is moot until answered). -->

<!-- quick-add-dictation → In Progress (SPAWNED iter 148). Unblocked: quick-add-modal landed iter 144 + lesson-audio-oversized-splitting landed iter 148 (index.ts/transcription boot wiring now stable on main). Adds a generic POST /api/transcribe (server/src/routes/transcribe.ts, task label `quick_add`, the thread-free audio→text portion of the chat voice route) + wires RecordButton into QuickAddModal. Design components.md QuickAdd dictation + INDEX amended & committed first. -->
<!-- llm-latency-tuning → In Progress (spawned iter 143; chat→haiku-4-5, suggestion_select→sonnet-4-6; model choices recorded DECISIONS iter 143). -->
- **suggestion-streaming** — text-streaming for suggestion generation end-to-end: an additive streaming path on the LlmProvider seam (no provider-specifics leaking, §6.7) → SSE/chunked to web → progressive render in Suggestions.tsx. llm-latency-tuning landed iter 143 (no longer in flight). Schema: none. [Should — **deferred iter 146, needs a focused seam-design pass first.** The `LlmProvider` seam (server/src/llm/types.ts) is non-streaming (`complete()`/`vision()` → `Promise<LlmResponse>`); adding streaming changes the shared interface EVERY llm task depends on (usage/cost logging + cache interplay must be re-thought for a streamed response). Suggestions also return a short structured selection, not long prose, so the streaming payoff here is marginal — a streaming seam, if built, would pay off more on chat/explanations. Not a tail-of-iteration spawn; give it its own design pass.]
<!-- nav-height-dividers → In Progress (SPAWNED iter 144). Quick-add-modal landed (AppNav collision resolved); design (`--nav-height` token + shell.md height/dividers spec) amended & committed first. Visual-only AppNav.tsx/.css + materialized token. -->
<!-- ask-mobile-composer → In Progress (SPAWNED iter 144). ask-history-delete landed (iter 143) + Ask.tsx is review-clean (review-07, 0 fixes) → no collision; ask.md region 2 amended & committed first. -->
- **ingest-mobile-guard** — **BLOCKED (escalated):** disable /ingest + its entry points on mobile (viewport-based). Contradicts GOAL §15 (PDF-ingestion Must must pass on a phone browser) → QUESTIONS.md "[PENDING] Disable /ingest on mobile". Build on the owner's answer. Would touch App.tsx (route guard) + Home.tsx + Review.tsx (Ingest entry points) + SiteHeader.tsx → collides with review-flow-polish on Home/Review. Schema: none. [Should — needs: human answer]

### Phase 1 fixes — remaining (human stepped away 2026-06-11; finish these, then design polish, then Phase 2)

<!-- mobile-nav-and-review-landing + library-forgot-button-layout → In Progress (spawned iteration 90, gate lifted) -->
<!-- notes-on-answers → In Progress (spawned iter 97; ask-chatbot/suggestions landed so the earlier collision concern is moot — it now forks from the post-UI-wave base). -->
- **review-04 follow-ups (dispositioned iter 99):** S1/S2/N1/N3 → **lesson-analysis-hardening** (In Progress, spawned iter 99); S4 → **nav-add-lessons-suggestions** (below, waits for notes-on-answers to free AppShell.tsx); B1 → ffmpeg escalation (QUESTIONS.md) + lesson-audio-oversized-splitting; S3 → folded into lesson-recording real-recording validation; N2 (queue ignores `TranscriptionError.retryable` → wastes 3 attempts on non-retryable errors, no $ spent) + N4 (AppNav indicator raw `4px`/`24px`/`3px` — reviewer marks it SANCTIONED extrapolation, shell.md doesn't dimension the pill) → recorded, no task. [review cadence]
- **review-05 follow-ups (dispositioned iter 106; fix wave now 4-of-4 LANDED → review-06 firing iter 114):** S1/S2/S3/N9/N11 → **suggestion-fixes** (Done iter 108, commit 9e334bd); S4/S5/S6/S8/N1/N3/N6/N8 → **chat-ask-fixes** (Done iter 108, commit 4306bec); N5/N10/N8-InsightRow → **lesson-notes-nonschema** (Done iter 112, commit 06b6a1a); S7 → **schema-gate-004** (Done iter 114, commit 37cef5e — migration 004 `source.duration_minutes`); N2 (get_page_context cosmetic stub), N4 (chat-added word is valid — auto-fill parity is a later nicety), N7 (chat token-streaming + "Earlier →" lazy-load — reviewer marks v1-acceptable) → recorded, no task. [review cadence]
<!-- lesson-notes-polish — FULLY DONE (both parts landed): (A) S7 → schema-gate-004 (merged iter 114, commit 37cef5e — migration 004 source.duration_minutes; server-only data-plumbing so Lessons list renders "· N min"); (B) lesson-notes-nonschema N5/N8/N10 (merged iter 112, commit 06b6a1a). Both in Done. -->
<!-- review-05's 8 should-fixes + the schema S7 are now all SHIPPED; review-06 (In Progress) audits the wave. -->
<!-- nav-add-lessons-suggestions (review-04 S4) → In Progress (spawned iter 99 once notes-on-answers landed and freed AppShell.tsx). -->
<!-- quiz-attempt-style-mixed — folded into schema-gate-003 (the CHECK change + code fix land together) -->
- **flaky-test watch — RESOLVED iter 119.** First seen iter 88 (one unreproduced single-test failure on the first post-merge check.sh run; re-runs green); recurred iter 118 as `web/src/screens/Triage.test.tsx:199` (`findByText("Everything sorted")` timed out on the first post-merge run; immediate re-run 571/571 green). Root cause = the two `findByText("Everything sorted")` assertions (lines 171/199) on RTL's default 1000ms timeout at the tail of a 3-step async chain, losing to CPU contention under the 68-file parallel suite. Trigger ("if it recurs, fix it") fired → **fix-flaky-triage-test** (test-only, explicit `{ timeout: 4000 }` on both) **LANDED iter 119** (commit 90ffaf4 — see Done). First post-merge main check.sh = 571 green with NO recurrence → watch **CLOSED**. If it ever recurs despite the 4000ms ceiling, that signals a deeper async-flow issue (not contention) — re-open then. [watch item → resolved]

### Design polish — AUTHORIZED iteration 55 (Apple-handoff bar; identity D0/D1 unchanged; runs after the Phase-1 fixes above because it rewrites shell/components/all screens)

<!-- design-polish-foundation integrated iter 75; home-nav-footer integrated iter 81 (commit e5e05b5). Per-screen sweeps decomposed into 3 file-disjoint tasks, all In Progress (spawned iter 81). -->
<!-- per-screen polish sweeps → split into polish-sweep-review-quiz / -library-grammar-lesson / -ingest-triage-system (In Progress). Home screen was built directly on the elevated bar by home-nav-footer; no sweep needed. -->


### Phase 2 (gate LIFTED per human master directive; GOAL §5 phase order; §11: lesson recordings → Ask → suggestions → voice). Resume here once Phase-1 fixes are in flight and capacity frees.

<!-- transcription-layer integrated iter 92 (commit 25438d6, 423 tests). lesson-recording-ingestion split (iter 92): BACKEND integrated iter 94 (commit ab9e83b, 444 tests). -->
<!-- lesson-recording-ui + ask-chatbot + suggestions → ALL integrated iter 97 (commits bf67b7b / 2396c66 / 818152e; trunk green 511 tests). The iter-94 seam pre-partition held — clean file-disjoint merges, no conflicts. -->
<!-- lesson-audio-oversized-splitting → DONE (LANDED iter 148, merge 96ef116; full entry in Done). The real ffmpeg `SplitAudio` is on main — the Phase-2 done-gate CODE. The 47-iteration env gate (ffmpeg) was cleared by the owner's iter-146 container rebuild; built + integrated iter 148. Real-recording validation (below) now proves it end-to-end. -->
<!-- lesson-recording real-recording validation → DONE (orchestrator-run iter 148; full entry in Done). The 24.8 MB owner fixture ran end-to-end through the REAL ffmpeg split path (2 chunks, 26.8 min) → live Whisper → stitch → lesson_analysis (15 insights: 6 flagged words, 2 corrections, 4 struggles, 3 topics), $0.41 total. review-04 S3 done (prompt seeded with the learner's literal code-switched self-flag cues; content-hash auto-bumps prompt_version). **Phase-2 *done* gate PROVEN — Phase 2 is complete.** Committed harness server/src/scripts/validate-lesson-audio.ts (mirrors validate-ingestion.ts; not in check.sh) for future re-validation. -->

<!-- ✅ PHASE 2 DONE (iter 148): construction complete earlier (transcription-layer · lesson-recording backend+ui · ask-chatbot · suggestions · voice-questions) + the two done-gate items now landed (lesson-audio-oversized-splitting CODE + real-recording validation) + review-05/06/07 waves all dispositioned & shipped. Next: Phase 3 (decompose when its fixtures land — docs/fixtures/mochi/ + gutenberg.org egress still absent) + Phase 4 polish; meanwhile the remaining iter-142 FEEDBACK feature tail (quick-add-dictation in flight, suggestion-streaming deferred, ingest-mobile-guard human-blocked). -->

<!-- voice-questions → DONE (merged iter 103, commit f3e6845, 554 tests). The LAST Phase-2 *construction* Must. Pre-partition (97005c5) held; worker stayed off app.ts/index.ts; clean merge. Phase-2 CONSTRUCTION is now complete (transcription-layer · lesson-recording-backend · lesson-recording-ui · ask-chatbot · suggestions · voice-questions all shipped). Phase-2 *done* still gated on the two human-action items below (ffmpeg QUESTIONS [PENDING] + lesson-audio fixtures absent) + review-05's findings. -->

### Phase 3+ (decompose when Phase 2 gate nears)

- Phase 3 (Gutenberg/KJV ingestion w/ archaic-aware rubric, English calibration + optional placement assessment, Mochi import — fixture-gated, docs/fixtures/mochi/ absent as of 2026-06-11) and Phase 4 polish [later]

Note: `no-design` and `codex-worker-engine` branches = human-owned (sandbox / spawn-engine infra work on bin/spawn + Dockerfile + firewall), not worker branches (see DECISIONS.md); ignore their ORPHAN status in list-agents.

## Done

<!-- Format: - **branch-name**: description (merged: YYYY-MM-DD HH:MM) [task/feature/release done] -->

- **quick-add-dictation**: FEEDBACK iter 142 — mic dictation inside the QuickAddModal (the audio-input item of the owner's iter-142 batch). Own commit 60ee9a8 = exactly the 7 brief files: NEW `server/src/routes/transcribe.ts` (generic `POST /api/transcribe` — multer 25 MB memoryStorage + `upload.single("file")`, audio-extension allowlist, injectable readAudioDuration seam → 400 invalid_audio, 503 transcription_unavailable when no service, `transcription.transcribe("quick_add", {data,filename,minutes})` → 502 on failure, trim → 422 empty_transcript, else `res.json({text})`; the thread-free audio→text slice of the chat voice route) + `transcribe.test.ts` (+92, mirrors the voice tests minus threads) + ONE app.ts register line (`registerTranscribeRoutes(app, db, opts.transcription)` after registerChatRoutes) + web: libraryApi `transcribeAudio(blob)` (FormData → POST /api/transcribe), QuickAddModal `transcribing` state + `handleRecorded` → `setTerm(text)` + a RecordButton beside the term field, QuickAddModal.css input+mic row, +QuickAddModal.test dictation case. NO shared type (trivial `{text}` typed inline → sidesteps the shared/dist coupling, so integrate needed no dist juggling). Verified diff-vs-brief (exact 7-file set; no check.sh/shared/RecordButton/chat-voice change) + read transcribe.ts substance before integrate; node_modules symlink present; killed the idle `done-quick-add-dictation` tmux window first; clean auto-merge (DECISIONS/TASKS/prompt/validate-script in the name-only diff = fork-drift off 46c8960 — branch forked before this pass's 2 base commits; integrate merged them cleanly, base kept for the state files). Design: components.md QuickAdd dictation + `transcribing` (amended iter 148). Schema: none. (merged: 2026-06-12 iter 148, merge cafd6d5 / commit 60ee9a8, check.sh green on main — 613 tests, +5) [task done]
- **lesson-recording real-recording validation** (orchestrator-run, no worker): **Phase-2 *done* gate PROVEN.** Ran the NEW `server/src/scripts/validate-lesson-audio.ts` (committed 7b99e9c; mirrors validate-ingestion.ts; not in check.sh) live against the owner fixture `docs/fixtures/lesson-audio/xaj-jnyr-wgv (2026-06-05 12_34 GMT-4).m4a` (24.8 MB, >24 MB). Result: the REAL ffmpeg `createFfmpegSplitAudio` splitter cut it into **2 chunks (26.8 min total)** → live OpenAI Whisper per chunk → stitched a 13,098-char transcript → `lesson_analysis` (fable-5) mined **15 lesson_insight rows** — 6 flagged words (llevar, desde hace, roto, escrito, enfadado, ventana de guillotina), 2 corrections, 4 struggle sentences, 3 topics — + 6 extraction_item triage candidates; 140s wall, **$0.4081** (transcription $0.16 + analysis $0.25; §13 single-op <$5). End-to-end proof the iter-99 review-04 **B1** escalation demanded (a real ~1 hr lesson exceeds 24 MB → must split→transcribe→stitch). **review-04 S3 done:** seeded `prompts/lesson_analysis.md` with the learner's *literal* code-switched self-flag cues seen in the real transcript ("I don't know", "I don't understand everything", "no sé", "¿cómo se dice…?") + a guard to ignore them as ordinary speech — the content-hash prompt_version auto-bumps. Trunk green 608 after both base commits. (done: 2026-06-12 iter 148, commit 7b99e9c) [Must — Phase 2 done gate ✅]
- **lesson-audio-oversized-splitting**: THE Phase-2 done-gate CODE — real ffmpeg `SplitAudio` for >24 MB lesson recordings. Own commit fe55e59 = exactly the 6 brief files: NEW `server/src/transcription/ffmpegSplit.ts` (frame-aware `ffmpeg -f segment -c copy -reset_timestamps 1`; segment_time computed from ffprobe-duration→bytes/sec at 92% of maxBytes; per-chunk minutes via ffprobe; a post-split size guard throws a clear TranscriptionError if a segment still exceeds the limit; temp-dir write/read/cleanup in `finally`; ≤-limit files pass straight through as one chunk) + `ffmpegSplit.test.ts` (live ffmpeg on synthetic audio) + the async seam (`SplitAudio` → `AudioChunk[] | Promise<AudioChunk[]>` in types.ts, `await this.splitAudio` in service.ts, service.test updated) + the injector at index.ts:36 (`{ splitAudio: createFfmpegSplitAudio() }`). Verified diff-vs-brief + read the ffmpegSplit.ts substance before integrate; killed the 2 idle branch tmux windows (false-refuse guard); node_modules symlinks present; ffmpeg 5.1.9 live. **First integrate FAILED (exit 5)** on a stale shared/dist contamination — `/workspace/shared/dist` (gitignored, resolved by the worktree's server typecheck via the symlinked node_modules) carried mochi's uncommitted `reviewFormat` from a prior build; rebuilt it from clean main → re-integrated green. Schema: none. (merged: 2026-06-12 iter 148, merge 96ef116 / commit fe55e59, check.sh green on main — 600 tests, +4) [task done — Phase-2 done-gate CODE; real-recording validation proves it end-to-end]
- **mochi-yesno-review**: Mochi-style binary "Do you know it?" review format (FEEDBACK iter 142). Own commit 71bb49b = exactly 10 files: `reviewFormat` KV preference (`mc` default · `yesno`) across shared/settings-api + db/settings-queries + routes/settings(+test) (NO migration — reuses the `setting` table) + ReviewCard `yesno` mode (.tsx/.css) + Review.tsx yesno render + landing `SegmentedControl` + Review.css tap-hint + System.test fixture. Worker committed mid-pass then died before writing `.worker-done` (list-agents-lag → reported STALE/1-commit). Independently verified diff-vs-brief (every element present, no migration, +8 tests) + ran check.sh in the worktree (604 green — matching the commit's self-attestation) → supplied the exit-0 marker the clean exit would have written + integrated (integrate re-ran check.sh as the real gate). Needed `/workspace/shared/dist` set to mochi's reviewFormat first (the same shared/dist coupling, mirror-imaged). Design: review.md §3.2b + components.md ReviewCard yesno (amended iter 146). Schema: none. (merged: 2026-06-12 iter 148, merge da9d6b5 / commit 71bb49b, check.sh green on main — 608 tests, +8) [task done]
- **ask-mobile-composer**: FEEDBACK iter 142 — mobile /ask composer redesign (owner: "full-width input pinned to the screen bottom, mic top-right of the input, send bottom-right"). Web layout-only: own commit abb7f68 = **EXACTLY** `web/src/screens/Ask.tsx` (the `.ask__composer` block, +12/-20) + `Ask.css` (+51/-6, mobile media-query) — verified diff-vs-brief before integrate. The `AppNav.css`/`tokens.css`/state-file entries in the `main..ask-mobile-composer` range-diff were pure **fork-drift** (branch forked at cceb748, before nav-height-dividers landed a659051 which added `--nav-height`+dividers to those files, so main was ahead — `git show abb7f68` touched neither); integrate's clean auto-merge kept main's versions. Behavior: mobile → full-width bottom-pinned TextInput, RecordButton absolutely top-right of the input, "Send" Button absolutely bottom-right, field reserves right/bottom padding so text never runs under either control, both ≥`--hit-target`; `bp-tablet`+ keeps the existing inline row; reuses the existing TextInput/RecordButton/Button + handleSend/handleRecorded (NO behavior change). node_modules symlinks present; killed the idle `done-ask-mobile-composer` tmux window before integrate; clean auto-merge. Design: ask.md region 2 (amended iter 144). Schema: none. (merged: 2026-06-12 iter 145, merge de32c2a / commit abb7f68, check.sh green on main — 596 tests) [task done]
- **nav-height-dividers**: FEEDBACK iter 142 — taller mobile AppNav + inter-cell dividers (owner request; supports §7 ≥44px targets, quiet per §3). Visual-only; own commit = exactly AppNav.css + web/src/styles/tokens.css: bar height `--hit-target`(44px)→new `--nav-height`(56px) token; a 1px `--color-rule` left-border on every cell except the first (`.app-nav__item:not(:first-child), .app-nav__add`) → hairline dividers between all 5 cells incl. the iter-144 Add cell. Pure CSS — NO AppNav.tsx/markup/test change (the existing tests incl. the Add-button cases are unaffected). Materialized `--nav-height` in tokens.css verbatim. Verified diff-vs-brief before integrate (only the 2 files; token present; dividers token-based); FINISHED mid-iteration (spawned+finished+integrated all within iter 144 — a one-rule CSS change); node_modules symlinks present; killed the idle `done-nav-height-dividers` window first (kept ask-mobile-composer's live one); clean auto-merge (design/state in the name-only diff = fork-drift off 4cddd71, stripped). Design: tokens.md(`--nav-height`)+shell.md(height/dividers)+INDEX committed 4cddd71 this iter. Schema: none. (merged: 2026-06-12 iter 144, merge a659051 / commit b913eae, check.sh green on main — 596 tests) [task done]
- **quick-add-modal**: FEEDBACK iter 142 — owner's persistent "+" quick-add, web-only, the app's FIRST modal/overlay. NEW `QuickAddModal` (web/src/components/QuickAddModal.tsx/.css/.test.tsx) reachable from a phone-AppNav trailing "+" **Add** `<button>` (glyph + "Add" label + aria-label "Add a word", conditional on the new `onQuickAdd` prop — a button, NOT a destination, no active state) + a desktop-SiteHeader quiet **+ Add** Button left of Ask; AppShell owns quickAddOpen/quickAddToast state, threads `onQuickAdd` to both nav surfaces, mounts the modal, raises the success Toast (held in AppShell so it survives the modal close) and refreshes the shared overview on add so the footer count stays live. Modal composes `createWord` (`POST /api/words`; server auto-defines a blank definition — NO endpoint/schema change) + TextInput(study,autoFocus) + SegmentedControl(es/en, default es) + Button(primary "Add"→"Adding…", quiet "Cancel") + help line; states: empty-term→inline "Enter a word or phrase." (no API call) · saving (busy/disabled) · success→onAdded+close · error→inline "Couldn't add…" stays open (no loss). A11y: role=dialog/aria-modal/aria-labelledby, Esc + backdrop-click (onMouseDown target-guard) close, Enter-in-field submits via a hidden submit, focus stored on open + returned to opener on close. Materialized the new `--color-scrim` token (light+dark, verbatim from the contract) in web/src/styles/tokens.css; panel uses the existing `--shadow-3` overlay elevation. Verified diff-vs-brief before integrating: file set = exactly the brief's components + tokens.css + the export index (the `components/index.ts` is the brief's export file, NOT the forbidden server index); NO forbidden file (Review/Home/Ask/chat/llm/server-index/app) touched; --color-scrim present in both token blocks; worker touched no state files (DECISIONS/TASKS in the name-only diff were fork-drift, stripped by integrate). FINISHED mid-iteration (spawned + finished + integrated all within iter 144 — the list-agents-lag pattern, fast); node_modules symlinks present; killed the idle `done-quick-add-modal` tmux window before integrate; clean auto-merge. Design: tokens.md/components.md/shell.md/INDEX.md amended & committed da783d6 this iter. Schema: none. (merged: 2026-06-12 iter 144, merge f10477a / commit e637939, check.sh green on main — 596 tests, +13) [task done]
- **review-07**: strong-model audit (opus/medium) of the 4 net-new product surfaces since the review-06 wave (range `820158a..HEAD`: prod-serve-web-guard, review-flow-polish, ask-history-delete, llm-latency-tuning; docs-only spa-routing-assessment + test-only fix-flaky-triage-test excluded, and the already-reviewed review-06 fixes inside the range not re-audited) → REVIEW.md: **SHIP — 0 blockers, 0 should-fixes, 1 nit + 2 watch-items** + an extensive line-by-line clean-bill. Each surface traced against real code + GOAL/ARCHITECTURE/design: prod guard (503-when-`index.html`-missing + boot warn + openable-URL log, `webDistDir` test-seam wiring threaded into both static + fallback, normal-serve no-regression, both unit tests); review flow (Card/ClozeCard instant `select()` grade mirroring Quiz `select()`, double-grade guard, "Check answer" gone / "Don't know" kept, D5 keyboard 1–4/Enter-once-answered/Space/D with the stale Enter→check binding removed, flip-card fallback intact, Home `?autostart=1` gated on due>0 + Review drained-queue→landing fallback, grade/persist errors surfaced via Toast); ask delete (transactional messages-then-thread atomicity, 404/204 contract, confirm-then-remove no-silent-loss + error Toast, open-thread-deleted UI reset, `×` aria-label + token colors + ≥44px tap target + focus ring, no-confirm asserted); llm tuning (2-line diff confirmed, 9 quality-critical defaults confirmed unchanged, **§14 ≥90%-accept drivers word_definition+quiz_cloze NOT downshifted → accuracy-safe**, §3 C1 path untouched, setting>env>default override precedence intact + DECISIONS-logged). Dispositioned iter 144: **NIT-1** (prod boot log `localhost` not LAN/Tailscale — reviewer "not worth a code change", README covers it) → no task; **WATCH-1** (suggestion accept-rate after suggestion_select→sonnet) + **WATCH-2** (Ask quality/tool-reliability after chat→haiku) → recorded watch items, both reversible via the config override, no action now. Integration used the review recipe: symlinked node_modules+web/node_modules into the review worktree (review workers never build → `tsc: not found`) + killed the idle `done-review-07` tmux window before integrate (left quick-add-modal's live window untouched). FINISHED mid-iteration (list-agents-lag: RUNNING/0-commits at pass start → committed REVIEW.md + dropped `.worker-done` during the pass). Worker's stale TASKS.md/design/ in the name-only diff were fork-drift (forked at ebcb184 before da783d6; integrate kept base's versions — its own commit touched only REVIEW.md). REVIEW.md git-rm'd. (merged: 2026-06-12 iter 144, merge a806ca7 / commit 2eef478, check.sh green on main — 583 tests) [task done]
- **review-flow-polish**: FEEDBACK iter 142 (two items, both web). (1) `/review` MC + cloze cards now grade the instant an option is picked — verified diff: both `Card` and `ClozeCard` replaced the `check` callback with `select(i)` (mirrors Quiz.tsx `select()`), the "Check answer" Button is gone, "Don't know" (quiet) kept, QuizOption onClick → `select(i)`, keyboard 1–4 pick+grade / Enter advances-once-answered (a new keyboard effect added to ClozeCard which lacked one). (2) Home hero "Start review" → `/review?autostart=1`; `Review` reads `?autostart` and jumps to phase `active` when due>0 (also fixed a pre-existing no-op `"landing":"landing"` ternary). Diff = exactly Home.tsx/.test + Review.tsx/.test (Review.css needed no change). Verified against the brief before integrating; FINISHED mid-iteration; node_modules symlinks present; killed the idle worker tmux window first; clean auto-merge (state-file fork-drift auto-resolved — base ahead, worker never touched them). Design: review.md (amended iter 142). Schema: none. (merged: 2026-06-12 iter 143, merge 71f6b4b / commit a8f5099, check.sh green on main — 583 tests) [task done]
- **ask-history-delete**: FEEDBACK iter 142 — single-click no-confirm delete of /ask history threads. Server: `deleteThread(db, id)` (db/chat-queries.ts) removes chat_message rows then chat_thread inside `db.transaction()` (atomic; chat_message has no ON DELETE CASCADE → no migration) returning existed?; `DELETE /api/chat/threads/:id` (routes/chat.ts) → 404 if missing else 204. Web: `handleDeleteThread` (Ask.tsx) optimistically drops the row from the list, resets to list view if the open thread is deleted, error toast on failure; a trailing `×` button per thread-list row (aria-label "Delete conversation"); `deleteThread(id)` in askApi.ts; Ask.css row-flex + delete-button styling. The brief's "shared chat-api type" was correctly skipped — a 204-no-body DELETE needs no typed response (avoided speculative scope). Diff = exactly the brief's server + web files; verified before integrating; FINISHED mid-iteration; node_modules present; killed idle window first; clean auto-merge. Design: ask.md (amended iter 142). Schema: none. (merged: 2026-06-12 iter 143, merge 56c46dd / commit 2348df1, check.sh green on main — 583 tests) [task done]
- **llm-latency-tuning**: FEEDBACK iter 142 — downshift the two latency-sensitive INTERACTIVE LLM tasks; keep quality-critical batch/grading tasks strong (§3 C1 / §14). Server-only: `TASK_DEFAULTS` in server/src/llm/service.ts — **chat** `claude-sonnet-4-6`→`claude-haiku-4-5` (conversational tutor, latency-dominant, tied to no accuracy metric; haiku supports its tools) + **suggestion_select** `claude-fable-5`→`claude-sonnet-4-6` (one tier only — suggestion acceptance is the tracked §14 metric); ALL 9 other defaults byte-identical (verified diff = exactly those 2 lines). + 2 new service.test.ts default-assertion cases; override-precedence tests intact; NO prompt changes (scope kept minimal). Model choices = mine per §13 (DECISIONS iter 143; the setting/env override path stays, so any model is still pinnable). FINISHED fast (trivial mechanical change); node_modules present; killed idle window first; clean auto-merge (the 11 non-service files in name-only were fork-drift — branch forked at 1a18d54 before the day's other 2 merges; main's newer versions won). Schema: none. (merged: 2026-06-12 iter 143, merge 43b711e / commit 707a850, check.sh green on main — 583 tests) [task done]
- **spa-routing-assessment**: FEEDBACK iter 142 — read-only MPA→SPA scoping assessment (the owner's "convert to SPA to kill full-page reloads on nav" ask). Confirmed real by the worker with cited file:line: the app ships as ONE React bundle but `App.tsx` routes by reading `window.location.pathname` on each render with NO router / NO `popstate` listener, and every in-app nav is a plain `<a href>` / `window.location.assign` → full document reload (re-download bundle + re-run every screen's mount fetch). Deliverable `docs/spa-routing-assessment.md` (22 KB): current architecture (cited), what "convert to SPA" means here, **Option A** (minimal hand-rolled history router + link interception — ~1–1.5 days, LOW risk, no new dep, the App.tsx matcher stays byte-for-byte, reversible — RECOMMENDED) vs **Option B** (adopt react-router — ~3–5 days, medium risk), a recommendation (A), and a worker-sized task breakdown if greenlit. Read-only confirmed: `git diff --name-only main..` = exactly `docs/spa-routing-assessment.md`, NO code touched (matches the brief). Finished mid-iteration (committed 2357572 + `.worker-done`=0 just after this pass began — the list-agents-lag/finished-mid-iteration pattern; opus/medium worker proc confirmed exited). Integrated with the assessment-worker recipe: symlinked node_modules + web/node_modules (assessment/review workers never build → integrate's check.sh would die on `tsc: not found`) + killed the idle `done-spa-routing-assessment` tmux window first (pane = bare zsh, no claude child). Branch check.sh green, clean auto-merge. **The refactor itself is a separate future OWNER decision — the doc says "Awaiting owner decision" — so NOT auto-spawned; greenlight Option A via FEEDBACK to build it.** Schema: none. (merged: 2026-06-12 iter 143, merge c472f7e / worker commit 2357572, check.sh green on main — 573 tests) [task done]
- **prod-serve-web-guard**: made the production web-serve self-diagnosing (FEEDBACK 2026-06-12 "can't view the website / nothing on :3000"). Server-only, 1 commit (9cb2854), exactly the 3 brief files (+62/-2): (1) `index.ts` logs the openable `http://localhost:PORT` on boot in production + a boot `logger.warn` when `web/dist/index.html` is missing; (2) `app.ts` SPA fallback now checks `fs.existsSync(index.html)` → returns a clear 503 plain-text "run `npm run build`" instead of a blank/opaque page (GOAL §6.9/§12 errors-surfaced); a `webDistDir` option was added to `createApp` purely as a test seam. Diff-audited against the brief before integrating (matches exactly; +2 app.test.ts cases covering the 503-when-missing path, and the normal-serve path preserved). Verified at spawn (iter 139) that the prod build+serve ALREADY worked end-to-end — no code defect; this is the robustness/observability follow-up to the website-down feedback. node_modules symlinks present (root + web; server resolves via the workspace hoist); killed the idle `done-prod-serve-web-guard` tmux window before integrate (false-refuse); branch check.sh green, clean auto-merge; first post-merge main check.sh = 573 green (+2). Schema: none. (merged: 2026-06-12 iter 141, commit 5e49a4f, check.sh green on main — 573 tests) [task done]
- **fix-flaky-triage-test**: de-flaked the two `findByText("Everything sorted")` assertions in `web/src/screens/Triage.test.tsx` (lines 171 + 199) — each now carries an explicit `{ timeout: 4000 }` (was RTL's default 1000ms), so the tail of the 3-step async decision chain no longer loses to CPU contention under the 68-file parallel suite (the flake first seen iter 88, recurred iter 118). Diff = exactly that 1 test file (+2/-2), NO `Triage.tsx`/component change (the component is correct — these tests guard a real fixed cursor/flow-order regression). Verified the diff matched the brief before integrating (both assertions, explicit 4000ms, nothing else); node_modules symlinks present in the worktree, killed the idle `done-fix-flaky-triage-test` tmux window first (exact-name false-refuse), branch check.sh green, clean auto-merge. First post-merge main check.sh = 571 green with **NO Triage flake recurrence** → fix confirmed working; flaky-test watch CLOSED. Schema: none. (merged: 2026-06-12 iter 119, commit 90ffaf4, check.sh green on main — 571 tests) [task done]
- **suggestion-deck-exclusion-fix**: review-06 **S2** (data-integrity), server-only. (A) Widened the deck-exclusion guard in `insertWordSuggestion` from `lemma_normalized = ? AND language` to `(lemma_normalized = ? OR term_normalized = ?) AND language = ?`, with `lemmaKey = normalize(payload.lemma ?? payload.term)` + `termKey = normalize(payload.term)` — so a deck word stored with NULL `lemma_normalized` is now caught by its (always-set) `term_normalized`, and a null-`payload.lemma` suggestion keys on its surface term; this restores review-05's never-re-suggest invariant. (B) Made `addWordToDeck` idempotent — a pre-check `SELECT id FROM word WHERE (lemma_normalized = ? OR term_normalized = ?) AND language='es'` returns the existing id instead of INSERTing a duplicate `word` row on an accepted re-suggestion. Worker commit (fdd27d3) touched exactly the 2 brief files (`db/suggestion-queries.ts` + `routes/suggestions.test.ts`, +103-line test expansion); verified each guard against real code before integrating; branch check.sh green, clean auto-merge (Lessons.tsx in the name-only diff was just fork-drift from main advancing past the branch's base — the merge correctly kept main's rounded version). Schema: none. (merged: 2026-06-12 iter 118, commit fc49a2e, check.sh green on main — 571 tests) [task done]
- **lessons-duration-rounding**: review-06 **S1**, web-only. The Lessons list label rendered the recording duration as a raw fractional float (migration 004 surfaced `source.duration_minutes` = seconds/60, an unrounded REAL → "· 58.4333 min"). Fix = `Math.round(row.durationMinutes)` at the DISPLAY boundary in `titleLine` (`web/src/screens/Lessons.tsx`), null guard preserved, stored/API value left precise → renders the spec's whole-number "· 58 min" (lessons.md). Diff = exactly the brief's 2 files (Lessons.tsx + Lessons.test.tsx, +1 test case); verified the fix matches the brief before integrating; clean exit marker, node_modules symlinks present, clean auto-merge. Schema: none. (merged: 2026-06-12 iter 118, commit 8b8c6d2, check.sh green on main — 571 tests, +1) [task done]
- **review-06**: strong-model audit (opus/medium) of the **review-05 fix wave** (range `5e8845c..HEAD`: suggestion-fixes 9e334bd, chat-ask-fixes 4306bec, lesson-notes-nonschema 06b6a1a, schema-gate-004 37cef5e incl. migration 004) → REVIEW.md: **0 blockers, 2 should-fix, 4 nits** + an extensive clean-bill (migration 004 additive/atomic/units-correct traced end-to-end; §16 no-silent-loss on suggestion add — addWordToDeck before status flip, 500 leaves row pending; notes bad-FK→clean 400; InsightRow "Ask about this" `/ask?new=1&kind=other&label=` wiring sound on both correction & struggle variants; wordDiff bounds correct; most-recent-50 chat window + determinism id-tiebreaks; Ask toast gated on the real receipt; lookup_word accent-strip; token discipline across the wave — all verified, traced not skimmed). Dispositioned iter 117 (DECISIONS.md): **S1** (Lessons label renders a raw float not whole "N min") → **lessons-duration-rounding**; **S2** (lemma-only deck-exclusion misses NULL-lemma words → re-suggest + dup `word` row via unchecked addWordToDeck) → **suggestion-deck-exclusion-fix**; **N1** (InsightRow 2px→`--space-1`=4px is a value change, no 2px token exists → accept the 4px offset), **N2** (ChatTurn Spanish regex heuristic — best-effort, no language tags without a contract change), **N3** (redundant `<br/>` before a block Spanish line → occasional extra blank line, cosmetic), **N4** (RecordButton bespoke `.record-btn__error` vs reuse `text-input__message--error`; microcopy+color correct — minor reuse nit) → recorded, no task. Integration used the standard review recipe: symlinked node_modules+web/node_modules into the review worktree (review workers never build → `tsc: not found`) + killed the idle `done-review-06` tmux window before integrate (exact-name/substring false-refuse). DECISIONS.md fork-drift stripped by integrate (base kept — `grep -c "Iteration 116"` = 1 post-merge). REVIEW.md git-rm'd. (merged: 2026-06-12 iter 117, commit 820158a, check.sh green on main — 566 tests) [task done]
- **schema-gate-004**: SCHEMA GATE migration 004 (ran ALONE) — review-05 **S7**, the last follow-up in the fix wave. Additive `ALTER TABLE source ADD COLUMN duration_minutes REAL` (nullable; no table rebuild, unlike 002/003's CHECK alters). `jobs/lessonAudioIngestion.ts` now persists the already-computed `minutes` (from `readAudioDuration`) into the same UPDATE that stores the transcript; `db/lesson-queries.ts:97` un-hardcoded (`durationMinutes: null` → reads `source.duration_minutes` from the list SELECT) so the Lessons list renders the spec's leading "· N min" (`web/src/screens/Lessons.tsx:39` already renders it when non-null). Server-only data-plumbing — no web/design change. +3 tests (migrate.test.ts asserts the column exists; lessonAudioIngestion.test.ts asserts persistence; routes/lessons.test.ts asserts the list surfaces the value). Diff = exactly the brief's 7 files; the 1-line DECISIONS.md fork-drift stripped by integrate (base kept, clean auto-merge). Schema: migration 004 only. (merged: 2026-06-12 iter 114, commit 37cef5e, check.sh green on main — 566 tests, +1; the migration applies cleanly on every fresh test DB per the run log, so green also proves 004 well-formed) [task done]
- **lesson-notes-nonschema**: review-05 N5 + N8 + N10 (the non-schema part of lesson-notes-polish; the review-05 fix wave is now 3-of-3 LANDED). **N5** — InsightRow correction underline now marks only the *changed word span* (a real `wordDiff` computes common prefix/suffix → `SpannedText` wraps changed words in `.insight-row__changed`, applied to both you/tutor lines) + the required trailing quiet "Ask about this" Button on BOTH correction & struggle variants. The button reuses the EXISTING Ask URL-seeding convention rather than editing Ask.tsx/askApi.ts: navigates to `/ask?new=1&kind=other&label=<insight text>`, which Ask.tsx already consumes (reads new/kind/label → builds a `ChatPageContext{kind:'other'}` → `createNewThread`) — cleaner than the brief's suggested askApi wiring, honors "do NOT edit Ask.tsx", and `'other'` is a valid `ChatPageContext` kind accepted server-side (verified). **N10** — `quizQuestionExists` added to notes-queries.ts + a pre-existence check in routes/notes.ts returns a clean 400 ("Quiz question not found") before the insert, replacing the SQLITE_CONSTRAINT_FOREIGNKEY 500. Did NOT touch app.ts (chat-ask-fixes owns its errorHandler). **N8** — InsightRow.css raw `2px` (×3: padding-top + two text-underline-offset) → `var(--space-1)`. Touched exactly its on-scope files (web InsightRow.tsx/.css/.test.tsx + server routes/notes.ts/.test.ts + db/notes-queries.ts); +82-line InsightRow test + 9-line notes test. Worker's DECISIONS.md fork-drift stripped by integrate (base kept). Schema: none. (merged: 2026-06-12 iter 112, commit 06b6a1a, check.sh green on main — 565 tests, +4) [task done]
- **chat-ask-fixes**: review-05 S4+S5+S6+S8+N1+N3+N6+N8 (Ask/chat + voice). S4 feed the model the most-RECENT 50 turns (`listRecentMessages`, newest-first then reversed) not the oldest 50; S5 `, id ASC`/`, id DESC` ordering tiebreaks on listMessages/listThreads (same-second determinism); S6 ChatTurn wraps Spanish lines in `.chat-turn__spanish` (`--font-study` italic per D4); S8 413 message drops the wrong hard-coded "50 MB"; N1 Ask toast gated on the real `toolReceipt.result`; N3 `lookup_word` accent-strips via `normalize()`; N6 RecordButton denied→inline error line + warning color only last ~15s; N8 Ask.css 64px→`--space-8`. +120-line chat.test.ts expansion. **list-agents misreported this branch STALE/0-commits at iter-108 start, but the work was already committed (6150517) & complete** — an accidental re-spawn self-resolved (the new worker recognized the committed work, ran check.sh green, exited without redoing anything) before integration. Touched exactly its 10 brief files (incl. app.ts errorHandler msg only); clean file-disjoint merge. Schema: none. (merged: 2026-06-12 iter 108, commit 4306bec, check.sh green on main — 561 tests, +3) [task done]
- **suggestion-fixes**: review-05 S1+S2+S3+N9+N11 (Suggestions feature only). S1 suggestion key → `suggestion.normalized_key` + deck-exclusion off the LEMMA (`word.lemma_normalized`) not the surface term; S2 attempt `addWordToDeck` BEFORE flipping status→added — on failure keep `pending` + return non-2xx so the UI surfaces it (GOAL §16); S3 calibration sample → `status IN ('known','mature')`; N9 uppercase only the "SUGGESTED ·" lead-in not the reason; N11 topic key → normalized topic name per ARCHITECTURE. Touched exactly its 5 brief files (server db/suggestion-queries.ts + routes/suggestions.ts(+test) + web Suggestions.tsx/.css); +125-line test expansion in suggestions.test.ts; clean file-disjoint merge (zero conflicts). Schema: none. (merged: 2026-06-12 iter 108, commit 9e334bd, check.sh green on main — 558 tests, +4) [task done]
- **review-05**: strong-model audit (opus/medium) of the Phase-2 UI/feature wave (lesson-recording-ui bf67b7b, ask-chatbot 2396c66, suggestions 818152e) + notes-on-answers (997b71a) + voice-questions (ef000e3), light pass on lesson-analysis-hardening (d11ecad) + nav-add-lessons-suggestions (f84507f) → REVIEW.md: **0 blockers**, 8 should-fixes (S1-S8), 11 nits + an extensive clean-bill (SQL safety, notes JOIN-through-quiz_question, chat tool-confirm safety, thread persistence, suggestion never-repeat, lesson read API, voice flow, component reuse, microcopy, token discipline all verified correct). Dispositioned iter 106 (DECISIONS.md): S1/S2/S3/N9/N11→**suggestion-fixes** (spawned); S4/S5/S6/S8/N1/N3/N6/N8→**chat-ask-fixes** (spawned); S7(schema-gated)/N5/N10/N8-InsightRow→**lesson-notes-polish** (Backlog); N2/N4/N7→recorded no-task. Integration used the iter-99 recipe: symlink node_modules+web/node_modules into the review worktree (review workers never build → `tsc: not found`) + kill the misnamed idle `done-review-05` tmux window before integrate (substring false-refuse). REVIEW.md git-rm'd. (merged: 2026-06-12 iter 106, commit 5e8845c, check.sh green on main — 554 tests) [task done]
- **voice-questions**: the LAST Phase-2 construction Must. Browser `MediaRecorder` clip → `POST /api/chat/threads/:id/voice` (multer) → `TranscriptionService.transcribe("voice_question", {data,filename,minutes})` (single pass-through chunk; pure-JS duration via `readAudioDurationMinutes`, no ffmpeg) → transcript becomes the chat user turn → normal assistant reply (server refactored to a shared `generateAssistantReply` helper used by both `/messages` and `/voice`, behavior-preserving) → one-tap SRS add reuses the EXISTING `add_word_to_deck` tool-confirm path. Web: `RecordButton` wired from placeholder to real MediaRecorder capture; `Ask.tsx` + `askApi.ts` post the clip. No `Source(type='voice_question')` row / no clip-to-disk — the transcript-as-chat-turn is the durable artifact (iter-100 scope decision; §5 "answered in a persistent Ask thread" met). Pre-partition (commit 97005c5) held — worker stayed entirely OFF app.ts/index.ts; merge had zero Auto-merging lines. Files (6, exactly the brief): server `routes/chat.ts` (+`chat.test.ts`) · web `Ask.tsx` + `askApi.ts` + `RecordButton.tsx` · shared `chat-api.ts`. Schema: none. (merged: 2026-06-12 iter 103, commit f3e6845, check.sh green on main — 554 tests, +5) [task done]
- **nav-add-lessons-suggestions**: review-04 S4 — added the shipped **Lessons** (/lessons) + **Suggestions** (/suggestions) to the desktop masthead `NAV` in `web/src/components/AppShell.tsx` (after Grammar, before Ingest), so they're reachable from the chrome (were unreachable since shipping iter 97). Exactly 2 insertions, in scope; phone AppNav + footer untouched. (merged: 2026-06-12 iter 99, commit f84507f, check.sh green on main — 549 tests) [task done]
- **lesson-analysis-hardening**: review-04 follow-up (S1+S2+N1+N3), server-only. `jobs/lessonAudioIngestion.ts` writeAnalysis now populates `level`+`example` and sets `likely_known = 0` for lesson-flagged words (were NULL → thinner triage rows than PDF/text; S2) + guards the delete-and-rewrite against clobbering decided triage rows (S1) + transcribes only when `source.transcript` IS NULL not "" (N3 re-spend); `prompts/lesson_analysis.md` returns level+example per flagged word (S2); `transcription/openai.ts` cost comment $0.40→$0.36 (N1). +108-line test for the S1 guard / N3 null-vs-empty / S2 population. Schema: none. (merged: 2026-06-12 iter 99, commit d11ecad, check.sh green on main — 549 tests, +4) [task done]
- **notes-on-answers**: per-answer self-note feature (FEEDBACK 2026-06-11). Server: `db/notes-queries.ts` + `routes/notes.ts` (CRUD on the `note` table — quiz_question_id + body; word/topic via JOIN through quiz_question) + `shared/src/notes-api.ts`. Web: post-grade "add a note" affordance (NoteAffordance) on Quiz + Lesson; `screens/Notes.tsx` browse + `notesApi.ts` + `/notes` route; secondary footer "Notes" link (no 5th top-nav item). Generation feedback: `jobs/quizGen.ts` + `jobs/lessonGen.ts` pull the word/topic's notes into the LLM prompt. Schema: none (note table in migration 003). Verified genuinely-alive iter 99 after a list-agents FALSE-STALE (misnamed tmux window — see DECISIONS); finished mid-iteration, clean merge (main's code was identical to its fork base). (merged: 2026-06-12 iter 99, commit 997b71a, check.sh green on main — 545 tests, +34) [task done]
- **review-04**: strong-model audit (opus/medium) of the design-polish pass + Phase-2 foundation backend → REVIEW.md (1 blocker, 4 should-fixes, 4 nits + an extensive clean-bill). Dispositioned iter 99 (DECISIONS.md): B1 (oversized audio can't transcribe — ffmpeg) → QUESTIONS.md ffmpeg escalation + the existing lesson-audio-oversized-splitting item; S1/S2/N1/N3 → lesson-analysis-hardening (spawned); S3 (seed mining prompt with owner's real flag phrases) → folded into lesson-recording real-recording validation (fixture-gated); S4 (masthead/footer omit Lessons+Suggestions) → nav-add-lessons-suggestions (queued, collides w/ notes-on-answers on AppShell.tsx); N2/N4 → recorded, no task. REVIEW.md git-rm'd. Integration needed: rename the mis-renamed `done-review-04` tmux window (held the live notes worker) + symlink node_modules into the review-04 worktree (review workers never build, so the gate's check.sh found no tsc). (merged: 2026-06-12 iter 99, commit b5c9126, check.sh green on main — 511 tests) [task done]
- **lesson-recording-ui**: web surfaces for lesson recordings + the read API the backend left unbuilt. NEW server `routes/lessons.ts` (GET list of lesson_audio recordings newest-first w/ summary counts; GET per-recording detail → `LessonRecordingView`) + `db/lesson-queries.ts` (group `lesson_insight` by type; pure reads). Web: `screens/Lessons.tsx` (list + per-recording insight detail per lessons.md — FLAGGED WORDS via WordEntry, CORRECTIONS/STRUGGLE via NEW `InsightRow` component, TOPICS→Grammar, collapsible transcript); Lesson-audio upload tab on `screens/Ingest.tsx` (+`ingestApi.ts` submitAudio w/ upfront cost estimate + progress); `screens/lessonsApi.ts`. Schema: none. (merged: 2026-06-12 iter 97, commit bf67b7b, check.sh green on main — 511 tests) [task done]
- **ask-chatbot**: persistent context-aware chat per screens/ask.md. `routes/chat.ts` (chat_thread/chat_message CRUD; page-context-seeded threads survive restarts; server-side tools add_word_to_deck/lookup_word/get_page_context w/ inline mutation confirmation; all turns via 'chat' LLM task + llm_call logging) + `db/chat-queries.ts` + `prompts/chat.md` + real `shared/src/chat-api.ts`. Web: `screens/Ask.tsx` + `askApi.ts` + NEW ChatTurn/ToolConfirm/RecordButton components (web/src/components/); plain "Ask" button in the chrome. RecordButton landed as a placeholder — voice-questions wires MediaRecorder→transcription. Schema: none. (merged: 2026-06-12 iter 97, commit 2396c66, check.sh green on main — 511 tests) [task done]
- **suggestions**: one-at-a-time word/grammar-topic proposals per screens/suggestions.md. `routes/suggestions.ts` (`suggestion` table w/ UNIQUE — nothing re-suggested incl. skips; LLM-selected proposals calibrated on known/mastered words via 'suggestion_select' task; add→deck/SRS or practice-queue, skip; pool-exhausted empty state) + `db/suggestion-queries.ts` + `prompts/suggestion_select.md` + real `shared/src/suggestions-api.ts`. Web: `screens/Suggestions.tsx` single-proposal surface + tally + Add/Skip thumb zone + cross-fade + `suggestionsApi.ts`. Schema: none. (merged: 2026-06-12 iter 97, commit 818152e, check.sh green on main — 511 tests) [task done]
- **lesson-recording-backend**: BACKEND slice of lesson-recording-ingestion (server + shared types; NO web/). `POST /api/sources/audio` (multer) → source(type='lesson_audio') → resumable ingestion job: transcribe via the landed `TranscriptionService` → store on `source.transcript` → `lesson_analysis` LLM task mines flagged-unknown words (→ extraction_item triage + lesson_insight 'flagged_word'), corrections, struggle sentences, topics covered (matched to grammar_topic + 'topic_covered'); per-topic `seenInLessons` wired into grammar-queries. Duration via pure-JS `music-metadata` (NO ffmpeg; >24 MB splitting deferred → clean job failure). Upfront Whisper cost estimate. Boot-wired TranscriptionService in index.ts. NOTE: built the INGEST + analysis side only — the lesson browse READ endpoints (list/detail → LessonRecordingView) were left unbuilt and are folded into lesson-recording-ui. Schema: none (lesson_audio + lesson_insight in 001). (merged: 2026-06-12 iter 94, commit ab9e83b, check.sh green on main — 444 tests, +14) [task done]
- **mobile-nav-and-review-landing**: built **AppNav** (phone bottom-bar nav per shell.md — fixed bottom, hit-target tall, paper+rule hairline, Home·Review·Library·Grammar, active = accent underline-pill) mounted in AppShell on phone widths (phone had ZERO nav before), and routed `/review` through AppShell so its pre-session landing / empty / finished states show standard chrome while the active card-answering run stays a focused takeover (mirrors Quiz config→play). FEEDBACK fix: "need mobile nav" + "navbar disappears on /review". Needed a post-merge trunk repair (commit 9433a18): the clean auto-merge onto transcription-layer's main duplicated a `transcription` object key in system-queries.ts + System.test.tsx (mobile-nav forked before transcription-layer, carried a zero-stub to satisfy the type) — fixed (merged: 2026-06-12 iter 92, commit 5b71b98, check.sh green on main — 430 tests) [task done]
- **transcription-layer**: provider-neutral `TranscriptionService` (server/src/transcription/, mirrors the LLM layer) + OpenAI Whisper adapter (`createOpenAiProvider`, whisper-1 default, `estimateWhisperCostUsd` @ $0.006/min); split→transcribe→stitch with an injectable `SplitAudio` seam (oversized-compressed splitting deferred to lesson-recording), retry/backoff, `transcription_call` logging; System-page transcription spend line (`SystemTranscriptionSpend`); mocked-provider tests. Server + System only; did NOT wire a TranscriptionService into boot (lesson-recording-backend does that) (merged: 2026-06-12 iter 92, commit 25438d6, check.sh green on main — 423 tests) [task done]
- **library-forgot-button-layout**: laid out the `/library` word-row status ("LEARNING") + "I forgot this" control horizontally (`.library__row` → flex; `.library__row-forgot` from `position:absolute` hover-reveal → always-visible `flex:0 0 auto` button beside the status, `--hit-target` tap target), fixing the overlap the owner reported. Token-only, one file (Library.css); WordDetail correctly left alone (no overlap there) (merged: 2026-06-12 iter 90, commit eea1376, check.sh green on main — 410 tests) [task done]
- **schema-gate-003**: SCHEMA GATE migration 003 (ran ALONE) — new `note` table (note → quiz_question_id + body; word/topic reached by join through the question); `quiz_attempt.style` CHECK gains 'mixed' + code stops fabricating a concrete style for mixed/lesson attempts (review-03 S5; quiz-attempt-style-mixed folded in); `chat_thread` rebuilt to Phase-2 spec (NOT NULL page_context + title). `transcription_call`/`chat_message`/`suggestion` already created to spec in 001 → left untouched. Lifted the schema gate: notes-on-answers, transcription-layer, ask-chatbot, suggestions all spawn afterward with "Schema changes allowed: none" (merged: 2026-06-12 iter 90, commit 032a75a, check.sh green on main — 410 tests) [task done]
- **polish-sweep-review-quiz**: `/review` + `/quiz` restyled to the elevated bar (visual/markup only) (merged: 2026-06-12 iter 88, commit 7871550, check.sh green on main — 408 tests) [task done]
- **polish-sweep-library-grammar-lesson**: `/library` + `/grammar` + lesson screens restyled to the elevated chrome bar (merged: 2026-06-12 iter 88, commit 83352ef, check.sh green on main — 408 tests) [task done]
- **polish-sweep-ingest-triage-system**: `/ingest` + `/triage` + `/system` restyled to the elevated bar (visual only) (merged: 2026-06-12 iter 88, commit 64e7644, check.sh green on main — 408 tests) [task done]
- **home-nav-footer**: built the `/` Home overview (HomeHero centerpiece + OverviewCard grid + activity band), wired SiteHeader/SiteFooter into the global shell across all non-session screens, and added the read-only `GET /api/overview` summary endpoint (counts shared by home + footer) (merged: 2026-06-12 iter 81, commit e5e05b5, check.sh green on main — 408 tests) [task done]
- **design-polish-foundation**: materialized the new additive tokens + restyled the base component library to the elevated bar + built the new chrome/home components (SiteHeader, SiteFooter, HomeHero, OverviewCard), each with co-located css + tests; not yet mounted into any screen (home-nav-footer does that) (merged: 2026-06-12 iter 75, commit efc0b98, check.sh green on main — 399 tests) [task done]
- **shared-api-refold**: refold web/src/screens/grammarApi.ts + systemApi.ts onto the shared web/src/api.ts client (review-03 S11) (merged: 2026-06-12 iter 74, commit 2403f77, check.sh green on main — 383 tests) [task done]
- **ingestion-progress-and-topic-link**: real per-page (N of M) progress on PDF reading + per-topic curriculum-building progress reported by the jobs (existing `job.progress` TEXT) and rendered on `/ingest` + `/grammar`; review-03 S12 deterministic page→topic linking from the page-classification LLM (seeded topic list wired into the prompt), NULL when unconfident (merged: 2026-06-12 iter 70, commits 16ef8b9 + c7715f1, check.sh green on main — 383 tests) [task done]
- **system-preferences**: System screen Preferences section (review-03 S8) — "Definitions on reveal · Spanish/English/Both" + "New cards per day · 10/20/40" SegmentedControls backed by GET/PUT `/api/settings`; definitionDisplay wired into reveal; nit#5 spend time-window + errors pager. Route stub pre-partitioned on base (e15ab3c) (merged: 2026-06-11 iter 69, commit dee8595, check.sh green on main — 378 tests) [task done]
- **design-polish-contract**: orchestrator-applied (no worker) — design-polish contract amendment per the authorized iter-55 directive (identity D0/D1 unchanged). New `screens/home.md` (`/` = navigable overview: HomeHero centerpiece + OverviewCard grid + activity band); `screens/shell.md` rewritten (sticky name-agnostic SiteHeader masthead+nav, AppNav reduced to Home·Review·Library·Grammar, new SiteFooter w/ utility links + live-count meta + theme toggle); `tokens.md` elevated additively (display type scale + tracking, --space-9/-10, --shadow-3, --color-paper-sunken, --color-accent-strong, --header-height, --motion-slow + dark overrides — no renames, in-flight workers unaffected); `components.md` gains SiteHeader/SiteFooter/HomeHero/OverviewCard + Button accent-strong hover. INDEX file map + Change-log. merriam-webster.com used as structural reference only (done: 2026-06-11, iteration 68) [task done]
- **triage-fixes**: triage advance never skips a pending word (root-cause fix); kept/known/skipped word leaves the queue immediately; post-Keep confirmation screen with "Done" → `/review`; `/triage?source=N` empty-queue redirect (merged: 2026-06-11 iter 67, commit 2591420, check.sh green on main — 371 tests) [task done]
- **library-mobile-separator**: `/library` SegmentedControl wraps to 2×2 on mobile with no divider between rows; added the row separator using existing tokens (merged: 2026-06-11 iter 66, commit def0ab8, check.sh green on main — 355 tests) [task done]
- **quiz-caching-and-bank**: reuse an existing unflagged cached `quiz_question` before regenerating (review-03 S1 / GOAL §6.4/§6.7); widened the def_match distractor pool + §6.4 distractor-quality rule (similar level, never a synonym — nit#10); `quiz_question_id` passed for ALL quiz-origin misses incl. def_match (S6). Server-only (quizGen.ts + routes/quiz.ts) (merged: 2026-06-11, commit 2069528, check.sh green on main — 355 tests) [task done]
- **lesson-grading-fixes**: free-text grading grounded on the user's actual answer + "you're close — here's how you'd say it" rephrase + "Partly right." verdict tier (review-03 S7) threaded through response + EMA weight; `quiz_grading` LLM task → sonnet/low effort for fast feedback; B2 lesson-attempt save failure surfaced (Toast + Retry-save, no silent loss); lesson-quiz action region pinned to thumb zone (S9); Review/lesson "Explain why" → `Button` (S10-review); progress bar final segment (nit#3) (merged: 2026-06-11, commit 0aca9a1, check.sh green on main — 351 tests) [task done]
- **quiz-ux-fixes**: `/quiz` play UX — instant green/red MC on click (no "Check answer", correctness client-side + async persist), removed "I don't know" on MC, cached "Explain why" on wrong answers via `Button`, default length 20→10, dropped the "All" deck segment, surfaced swallowed answer/attempt errors, progress bar reaches final segment, 680px→`--measure-reading` (review-03 B1/S2/S3/S4/S10-quiz + nits #3/#4) (merged: 2026-06-11, commit 1e6ea73, check.sh green on main — 348 tests) [task done]
- **docs-and-demo**: Phase-1 app docs — docs/README.md cold start (clone→run→phone via LAN/Tailscale, "Where your data lives", backup/restore), root README pointer, TODO-LATER.md, docs/demo.md, LLM hot-swap proof; covers review-01 #9 (merged: 2026-06-11, commit 6af5a1e) [task done]
- **review-03**: audit of the 5 integrations since review-02 → REVIEW.md (2 blockers, 12 should-fixes, 11 nits + clean-bill verification); all dispositioned in DECISIONS.md iteration 55, REVIEW.md removed (merged: 2026-06-11, commit 1c633a9) [task done]

- **grammar-lessons-quizzes**: lesson generation job (lessonGen.ts via `grammar_lesson` LLM task + prompts/grammar_lesson.md), Lesson screen (reading column → practice flow), MC/fill-in/conjugation/free-text answering with LLM grading (`quiz_grading` task + prompts/quiz_grading.md, local grading w/ LLM fallback), "explain why" everywhere, mastery EMA from attempts, Grammar home topics link into lessons (merged: 2026-06-11 14:53, check.sh green on main — 347 tests) [task done]

- **design-phase2-extension**: orchestrator-applied (no worker) — Phase 2 design contract: screens/lessons.md + ask.md + suggestions.md; ChatTurn/ToolConfirm/RecordButton/InsightRow in components.md; shell Ask-button + nav rules; Ingest lesson-audio tab; System transcription spend; Grammar "seen in N lessons"; D5 keys + microcopy; INDEX file map + Change log. No new tokens (done: 2026-06-11 ~14:50, iteration 50) [task done]

- **quiz-engine-ui**: Quiz config/play/results per design/screens/quiz.md (routes/quiz.ts, db/quiz-queries.ts, shared/quiz-api.ts, jobs/quizGen.ts, prompts/quiz_cloze.md, web Quiz.*) + review-02 #8: Review screen "Explain why" + cached-cloze mix-in into due queue (merged: 2026-06-11 14:37, check.sh green on main — 328 tests) [task done]

- **system-page**: System screen per design/screens/system.md (recent errors from error_log, job statuses, LLM spend from llm_call, DB/backup status) + daily timestamped DB backup job (jobs/backup.ts, enqueue-if-due on boot + daily interval); routes/system.ts + db/system-queries.ts + shared/system-api.ts + web System screen + /system route (merged: 2026-06-11 21:08, check.sh green on main — 302 tests) [task done]

- **review-02-fixes**: review-02 blocker #1 (confirmBatch within-batch + homograph duplicates surfaced as dedupeHits, no 500/rollback) + should-fixes #2–#7 + nits N1/N2/N4/N6/N7/N10/N12 — triage dedupe/bulk/summary, SRS demote-creates-card/distractors-from-deck/precision, review thumb zone fixed action region, shared web api client (merged: 2026-06-11 13:57, check.sh green on main — 288 tests) [task done]

- **grammar-curriculum**: curriculum seeding job (grammarSeed.ts) via grammar_curriculum LLM task + prompts/grammar_curriculum.md → grammar_category/grammar_topic rows; grammar home screen per design/screens/grammar.md with mastery-derived practice queue; review-01 #8 page→topic linking in pdfIngestion (merged: 2026-06-11 13:45, check.sh green on main — 274 tests) [task done]
- **library-ui**: library screen + word CRUD API (/api/words GET/POST/PATCH/DELETE, accent-insensitive search, filters, manual add with word_definition auto-fill, delete with history retained, forgot-this, WordDetail provenance) (merged: 2026-06-11 13:26, check.sh green on main — 254 tests) [task done]
- **raw-text-ingestion**: POST /api/sources/text + text_ingestion job + text_extraction LLM task, Ingest screen (paste panel + PDF drop), language select/auto-detect; carried review-01 #5 + #13 (merged: 2026-06-11 13:26, check.sh green on main — 254 tests) [task done]
- **review-02**: audit of integrations 5–9 → REVIEW.md: 1 blocker (confirmBatch duplicate 500), 7 should-fixes, 12 nits + clean-bill verification; all dispositioned in DECISIONS.md 2026-06-11, REVIEW.md removed (merged: 2026-06-11 13:27) [task done]
- **review-01-fixes**: six review-01 findings fixed — migration 002 quiz_question exactly-one CHECK (table rebuild), Multer 413 file_too_large, nowIso ms strip, triage retry ≥44px tap target, WordEntry hero breakpoint → 640px, Button busy default "…" (merged: 2026-06-11 13:04, check.sh green on main — 200 tests) [task done]
- **pdf-ingestion-live-validation**: live end-to-end ingestion of both docs/fixtures/workbook/ PDFs with real Anthropic vision calls; docs/validation-pdf-ingestion.md with per-page outcomes + llm_call cost; fixes: §6.1 {{calibration_sample}} templating, pending rows drop ingestion-time word_id, extractJson fence strip; validate-ingestion.ts script (opt-in, not in check.sh) (merged: 2026-06-11 13:05, check.sh green on main — 200 tests) [task done]
- **review-ui**: SRS review screen at /review per design/screens/review.md over the merged SRS API: due cards, API-assigned direction, MC default + flip-card fallback, three grades, both-definition reveal, "I forgot this" demote (merged: 2026-06-11 13:05, check.sh green on main — 200 tests) [task done]
- **triage-ui**: triage screen end-to-end (routes/triage.ts + db/triage-queries.ts + shared/src/triage-api.ts + web Triage screen): know/learn/skip, batch confirm materializes words with confirm-time lemma dedupe surfaced for keep/merge (merged: 2026-06-11 12:41, check.sh green on main — 179 tests) [task done]
- **srs-api-wiring**: SM-2 engine wired to DB + HTTP: GET /api/decks/:id/due with new-card promotion, POST /api/reviews writing review_log + card_state, POST /api/words/:id/demote; shared/src/srs-api.ts types (merged: 2026-06-11 12:40, check.sh green on main — 179 tests) [task done]
- **review-01**: audit of integrations 1–4 → REVIEW.md, 14 findings (3 contract should-fixes, 3 GOAL-fidelity, 3 design, 5 nits) + extensive clean-bill verification; all findings dispositioned in DECISIONS.md 2026-06-11, REVIEW.md removed (merged: 2026-06-11 12:41) [task done]
- **pdf-ingestion-pipeline**: upload endpoint + ingestion job: PDF stored as source, per-page vision extraction via LlmProvider layer + anthropic adapter, page classification → source_page, candidates → extraction_item; per-page retry; prompts in /prompts; mocked-provider tests (merged: 2026-06-11 11:50, check.sh green on main after npm install — 142 tests; re-spawn 1 resolved the package-lock conflict) [task done]

- **design-foundation**: design/tokens.md materialized as token stylesheet + components.md base components built as composable library in /web, with tests (merged: 2026-06-11, check.sh green on main after npm install — 116 tests) [task done]

- **sm2-engine**: /server/src/srs/ pure SM-2 functions — grade application, manual demotion, maturity ≥21d, review queue builder with new-cards/day promotion; unit tests; new files only (merged: 2026-06-11 18:36, check.sh green on main, 61 tests) [task done]

- **bootstrap**: monorepo scaffold (server/web/shared/prompts) + check.sh + DB layer with migration runner + 001_init.sql (full finalized ARCHITECTURE.md schema) + persistent job queue + structured logger/error_log + config + healthcheck; 20 tests green (merged: 2026-06-11 18:18, after 5 re-spawns — all integration artifacts, not work failures; full history in git log of this file) [task done]

- **arch-critique**: critic pass over ARCHITECTURE.md + seeded design contract (2026-06-10) — all 13 findings adopted and reconciled directly by the orchestrator; branch abandoned after reconciliation (integrate exit 7 was a base-drift artifact; real diff = CRITIQUE.md only — see DECISIONS.md) [task done]

## Blocked

<!-- Format: - description — REASON — needs: what would unblock it -->

<!-- (none) — the UI-polish discussion was answered iteration 55; design polish is now authorized and queued in the Backlog. -->
