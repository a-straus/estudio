# TODO-LATER

Deferred wishes — features the owner consciously cut from Estudio v1. Each is
**deliberately cut from v1, owner-acknowledged**, not forgotten and not a bug.
They are recorded here so the scope of v1 stays clear and so a future phase can
pick them up.

## TTS / audio pronunciation playback

Tap a word or example to hear it spoken. Deliberately cut from v1,
owner-acknowledged — v1 is text-only; no audio synthesis or playback ships.

## SMS/email word-capture inbox ("text the app a word")

Capture vocabulary on the go by texting or emailing a word to the app, which
would land in the triage inbox. Deliberately cut from v1, owner-acknowledged —
v1 captures material only through the in-app Ingest screen (PDF upload, pasted
text).

## Combined vocab + grammar quizzes

A single quiz that mixes vocabulary cards and grammar-lesson questions in one
session. Deliberately cut from v1, owner-acknowledged — in v1 vocabulary quizzes
(`/quiz`) and grammar-lesson quizzes (inside each lesson) are separate flows.

## Additional subjects beyond Spanish/English vocab

Extending the app to other subjects or language pairs beyond Spanish/English
vocabulary and grammar. Deliberately cut from v1, owner-acknowledged — v1 is
scoped to Spanish/English study only.

## Full word-select-to-add

Selecting an arbitrary span of text anywhere in the app to add it as a new word.
Deliberately cut from v1, owner-acknowledged — v1 ships the **reduced** version:
words are added through triage of an ingested source, or typed manually in the
Library ("add word", with auto-fill definition). Free-form select-anywhere
capture is deferred.
</content>
