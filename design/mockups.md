# D6 · Reference mockups

<!--
Optional. If static HTML/CSS mockups exist, list them: each embeds the
tokens.md token block verbatim and uses components.md component names as
kebab-case class names so agents can map markup → component 1:1. Where a
mockup exists it is the styling source of truth; where none does, the
design files are.
-->

| File | Implements | Shows |
|---|---|---|
| | | |
