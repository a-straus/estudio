import type { Express } from "express";
import type { DB } from "../db/db.js";

// Library/word CRUD routes — implemented by the library-ui task.
export function registerWordRoutes(_app: Express, _db: DB): void {}
