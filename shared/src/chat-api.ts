// Ask chat (Phase 2) API request/response + DTOs.
//
// STUB — pre-partitioned by the orchestrator and already re-exported from
// shared/src/index.ts. The `ask-chatbot` task OWNS and replaces this file with
// the real ChatThread / ChatMessage / tool-call DTOs (screens/ask.md). Do NOT
// edit shared/src/index.ts (it is a shared seam) — adding types here is enough.
export type ChatApiStub = Record<string, never>;
