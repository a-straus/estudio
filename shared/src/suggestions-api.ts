// Suggestions (Phase 2) API request/response + DTOs.
//
// STUB — pre-partitioned by the orchestrator and already re-exported from
// shared/src/index.ts. The `suggestions` task OWNS and replaces this file with
// the real Suggestion DTOs (screens/suggestions.md). Do NOT edit
// shared/src/index.ts (it is a shared seam) — adding types here is enough.
export type SuggestionsApiStub = Record<string, never>;
