/**
 * Ask (Phase 2) — STUB. App.tsx already routes `/ask` here through AppShell
 * (mirroring Quiz: handle the mobile full-screen-takeover/session-bar look
 * internally via CSS, do not refactor App.tsx). The `ask-chatbot` task OWNS
 * and replaces this file per design/screens/ask.md. Do NOT edit App.tsx.
 */
export function Ask() {
  return null;
}
