/**
 * Lessons (Phase 2) — STUB. App.tsx already routes `/lessons` here through
 * AppShell. The `lesson-recording-ui` task OWNS and replaces this file per
 * design/screens/lessons.md (lesson list + per-recording insight detail). Do
 * NOT edit App.tsx.
 */
export function Lessons() {
  return null;
}
