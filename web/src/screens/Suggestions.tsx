/**
 * Suggestions (Phase 2) — STUB. App.tsx already routes `/suggestions` here
 * through AppShell. The `suggestions` task OWNS and replaces this file per
 * design/screens/suggestions.md. Do NOT edit App.tsx.
 */
export function Suggestions() {
  return null;
}
